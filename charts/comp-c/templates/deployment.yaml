apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "comp-c.fullname" . }}
  labels:
    {{- include "comp-c.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "comp-c.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "comp-c.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 5000
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "comp-c.fullname" . }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /alive
              port: 5000
          readinessProbe:
            httpGet:
              path: /alive
              port: 5000
